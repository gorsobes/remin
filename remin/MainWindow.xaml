<Window x:Class="remin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Простое WPF-приложение" Height="200" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputTextBox" Width="200" Height="25" 
                 Margin="10" HorizontalAlignment="Center"
                 TextChanged="InputTextBox_TextChanged"
                 KeyDown="InputTextBox_KeyDown"/>

        <Button x:Name="ShowTextButton" Content="Показать текст"
                Width="150" Height="30" Margin="10" Grid.Row="1"
                HorizontalAlignment="Center" Click="Button_Click"
                IsEnabled="False"/>
        <!-- Кнопка отключена по умолчанию -->

        <TextBlock x:Name="OutputTextBlock" FontSize="16"
                   Margin="10" Grid.Row="2" HorizontalAlignment="Center"/>
    </Grid>
</Window>


